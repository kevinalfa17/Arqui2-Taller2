Para el ejercicio 2 (vector de maximos)

gcc max.c -o max
./max

Llenar vectores segun se le indique

Para el ejercicio 3 (multiplicacion de matrices)

gcc matrix.c -o matrix
./matrix

Llenar matriz 1 y 2 segun se le indique (F = fila C = columna)
